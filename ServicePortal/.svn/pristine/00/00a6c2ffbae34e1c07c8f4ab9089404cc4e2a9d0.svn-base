import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BandwidthcalenderComponent } from './bandwidthcalender.component';

describe('BandwidthcalenderComponent', () => {
  let component: BandwidthcalenderComponent;
  let fixture: ComponentFixture<BandwidthcalenderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BandwidthcalenderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BandwidthcalenderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
