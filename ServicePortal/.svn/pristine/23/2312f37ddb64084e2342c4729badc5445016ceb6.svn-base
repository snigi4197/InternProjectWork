import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BcTabDataComponent } from './bc-tab-data.component';

describe('BcTabDataComponent', () => {
  let component: BcTabDataComponent;
  let fixture: ComponentFixture<BcTabDataComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BcTabDataComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BcTabDataComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
