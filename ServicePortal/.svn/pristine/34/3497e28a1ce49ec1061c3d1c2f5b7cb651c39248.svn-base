import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BodTabDataComponent } from './bod-tab-data.component';

describe('BodTabDataComponent', () => {
  let component: BodTabDataComponent;
  let fixture: ComponentFixture<BodTabDataComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BodTabDataComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BodTabDataComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
