import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ServiceSchemaComponent } from './service-schema.component';

describe('ServiceSchemaComponent', () => {
  let component: ServiceSchemaComponent;
  let fixture: ComponentFixture<ServiceSchemaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ServiceSchemaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ServiceSchemaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
