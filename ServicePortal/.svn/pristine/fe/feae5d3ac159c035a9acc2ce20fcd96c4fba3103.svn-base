import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DivfieldsComponent } from './divfields.component';

describe('DivfieldsComponent', () => {
  let component: DivfieldsComponent;
  let fixture: ComponentFixture<DivfieldsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DivfieldsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DivfieldsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
